# 3FUI-Studios 项目索引

这是一个基于 GitHub Pages 的静态网站后端结构，用于存储视频压制项目的元数据。

## 📂 目录结构

- **projects/**: 数据源。请在这里提交你的压制项目 JSON。
- **frontend/**: 前端工程源码 (Vue 3 + Vite + Tailwind CSS)。
- **public/**: 网站发布目录。
  - `api/`: Python 脚本生成的 JSON 数据。
  - `index.html` & `assets/`: 前端构建产物。

## 🚀 如何提交新项目 (How to Contribute)

### 1. 准备文件
在 `projects/2026/` (或当年年份) 目录下创建一个新的 JSON 文件，建议命名为 `你的ID_作品名.json`。

### 2. 填写内容
请复制以下极简模板：

```json
{
  "basic": {
    "title": "Big Buck Bunny [AV1 1080p]",
    "description": "这里写你的压制心得、参数说明等...",
    "tags": ["AV1", "1080p", "SVT-AV1"]
  },
  "meta": {
    "author": "你的ID或昵称"
  },
  "source": {
    "url": "https://media.xiph.org/video/derf/"
  },
  "download": {
    "url": "https://你的下载链接.com/file.mkv"
  }
}
```

### 3. 提交 PR (Pull Request) 流程

如果你是第一次使用 GitHub 提交代码，请遵循以下步骤：

1.  **Fork 仓库**：点击右上角的 `Fork` 按钮，把这个仓库复制到你自己的账号下。
2.  **创建文件**：
    *   在你 Fork 后的仓库中，进入 `projects/2026/` 文件夹（如果年份文件夹不存在，请新建）。
    *   点击 `Add file` -> `Create new file`。
    *   **文件名规范**：请使用英文、数字或下划线，例如 `av1_test_v1.json` 或 `user_project_01.json`。**不要使用中文文件名**。
    *   将上面的 JSON 模板复制进去，修改为你自己的内容。
3.  **提交更改 (Commit)**：
    *   在页面底部，填写 Commit message（例如：`Add: 新增 Big Buck Bunny 压制项目`）。
    *   点击 `Commit changes`。
4.  **发起请求 (Pull Request)**：
    *   回到项目首页，你会看到一个提示条，点击 `Compare & pull request`。
    *   确认标题和描述无误后，点击 `Create pull request`。
5.  **等待审核**：
    *   GitHub Actions 会自动检查你的 JSON 格式是否正确。
    *   如果检查通过（显示绿色 ✅），管理员会尽快合并你的提交。
    *   如果检查失败（显示红色 ❌），请点击 Details 查看错误原因并修改。

## 🛠️ 本地开发与项目架构

本项目采用了 **Jamstack** 架构（JSON 数据源 + Python 构建 + Vue 3 前端）。

### 1. 核心模块说明
*   **`projects/` (Data)**: 核心数据源。
*   **`scripts/build_indexes.py` (Build Data)**:
    *   Python 脚本，负责遍历 `projects/`，生成 `public/api/` 下的 JSON 索引文件（包含 `all.json` 用于前端搜索）。
*   **`frontend/` (Frontend Source)**:
    *   Vue 3 + Vite + Tailwind CSS 工程。
    *   `npm run dev`: 启动开发服务器。
    *   `npm run build`: 构建前端到 `public/` 目录。
*   **`public/` (Distribution)**:
    *   最终发布的静态网站目录。

### 2. 运行本地环境

#### 环境准备
*   Node.js (v18+)
*   Python 3

#### 步骤
1.  **构建数据** (如果有新的 JSON 项目):
    ```bash
    python scripts/build_indexes.py
    ```
2.  **开发前端**:
    ```bash
    cd frontend
    npm install
    npm run dev
    ```
3.  **构建生产版本**:
    ```bash
    cd frontend
    npm run build
    ```
4.  **预览生产版本**:
    ```bash
    python -m http.server 3000 --directory public
    ```
